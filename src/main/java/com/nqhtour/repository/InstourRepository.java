package com.nqhtour.repository;

import com.nqhtour.entity.InstourEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InstourRepository extends JpaRepository<InstourEntity, Long> {
}
